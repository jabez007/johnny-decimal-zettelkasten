# GENERATED FILE — DO NOT EDIT.
# Agent entries are generated from .agents/agents/*.md.
# Regenerate with: ./scripts/sync-assets.sh

[features]
hooks = true

[agents]
max_depth = 1


[agents."daily-reviewer"]
description = "Extracts durable knowledge from transient daily notes. Identifies emerging concepts for the JDex."
config_file = "./agents/daily-reviewer.toml"
nickname_candidates = ["Iris", "Clio"]

[agents."flashcard-generator"]
description = "Generates spaced-repetition flashcards (Q&A or Cloze) for Obsidian notes."
config_file = "./agents/flashcard-generator.toml"
nickname_candidates = ["Anki", "Mnemosyne"]

[agents."librarian"]
description = "General knowledge steward for Johnny-Decimal/Zettelkasten Obsidian vaults. Use for general questions, ID lookups, and coordinating vault maintenance."
config_file = "./agents/librarian.toml"
nickname_candidates = ["Ada", "Linnaeus"]

[agents."source-distiller"]
description = "Processes long-form sources (articles, transcripts) into atomic, concept-oriented permanent notes."
config_file = "./agents/source-distiller.toml"
nickname_candidates = ["Scribe", "Tarski"]

[agents."vault-auditor"]
description = "Specialized in auditing Obsidian vault link health, identifying orphans, broken links, and connection opportunities."
config_file = "./agents/vault-auditor.toml"
nickname_candidates = ["Athena", "Lintel"]

[agents."vault-cleaner"]
description = "Specialist in vault hygiene. Detects duplicates, misplaced notes, and naming inconsistencies; proposes consolidation and restructuring."
config_file = "./agents/vault-cleaner.toml"
nickname_candidates = ["Broom", "Theseus"]

[agents."vault-scaffolder"]
description = "Expert in constructing new Johnny-Decimal structures. Guides the creation of systems, areas, and categories."
config_file = "./agents/vault-scaffolder.toml"
nickname_candidates = ["Grid", "Keystone"]

[agents."vault-synthesizer"]
description = "Specialist in knowledge compounding and synthesis. Identifies contradictions, synergies, and gaps; proposes \"Bridge\" or \"Synthesis\" notes."
config_file = "./agents/vault-synthesizer.toml"
nickname_candidates = ["Hermes", "Bridge"]

[[hooks.SessionStart]]
matcher = "startup|resume"

[[hooks.SessionStart.hooks]]
type = "command"
command = 'bash "$(git rev-parse --show-toplevel)/.codex/hooks/session-start.sh"'
timeout = 30
statusMessage = "Loading Agent Memory context"
